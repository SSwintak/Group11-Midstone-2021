#include "Room.h"

Room::Room(string name_): name(name_){

	//for placeholder, remove it with fileloading later
	//for (int i = 0; 0 < sizeof(itemList); i++) {
	//	itemList[i] = nullptr;
	//}
}

Room::~Room()
{
}
